# Private TestFlight release

The authoritative specification is [GitHub issue #1](https://github.com/mustnotsin/QazaqVocab/issues/1). Read its full body and comments before changing release scope.

Implementation is split into child issues `#2`–`#14`. GitHub's native `blocked by` relationships define the execution frontier; work only tickets whose blockers are closed.

The first ticket is [#2: Build the shared featured-word foundation](https://github.com/mustnotsin/QazaqVocab/issues/2).
